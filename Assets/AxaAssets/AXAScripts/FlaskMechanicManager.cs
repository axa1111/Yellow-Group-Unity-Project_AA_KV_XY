using UnityEngine;
//this script will manage the flask interaction and animation being triggered
public class FlaskMechanicManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }

    public void FlaskMechanic()
    {

    }
    
    //use coroutines for the animation and do the texture change of the towel here
}
